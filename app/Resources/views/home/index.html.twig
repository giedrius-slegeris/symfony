{% extends 'init.html.twig' %}

{# set page title #}
{% block title %}
  {{ title }}
{% endblock %}

{% block body %}

<div class="row">
  <div class="col-sm-12">
    <h1>Home</h1>

    {# greet with URL parameter #}
    {% if name is defined %}
      <h3>Hello {{ name }}!</h3>
    {% endif %}

  </div>
</div>

{% endblock %}
